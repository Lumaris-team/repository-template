{
  "name": "repository-template",
  "version": "1.0.0",
  "description": "Cloudflare Workers repository template",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": ">=20"
  },
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Tests not configured yet\" && exit 0"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  },
  "dependencies": {}
}
